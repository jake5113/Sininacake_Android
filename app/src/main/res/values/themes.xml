<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sinina" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Sinina" parent="Base.Theme.Sinina" />

    <style name="AppTheme.sinina" parent="Base.Theme.Sinina">
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_transparent </item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="windowSplashScreenBackground">@color/white</item>

        <item name="postSplashScreenTheme">@style/Base.Theme.Sinina</item>
    </style>
</resources>